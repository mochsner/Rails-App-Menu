class App < ApplicationRecord
	# Relational table name is 'apps'
	# Override possible via self.table_name = 'any_other_name'
	# attr_accessible :name, :description, :Color, :DefaultStatus, :Link
	# belongs_to :users
end
